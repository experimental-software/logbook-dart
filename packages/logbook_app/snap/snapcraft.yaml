name: logbookapp
version: "0.2"
summary: App for chronological note taking
description: Allows to create note directories with the current date and time.

confinement: devmode
base: core18
grade: stable

plugs:
  file-system-access:
    interface: system-files
    read:
      - /home
    write:
      - /home

apps:
  logbookapp:
    command: logbookapp
    extensions: [flutter-stable]
    plugs:
      - file-system-access

parts:
  logbookapp:
    source: .
    plugin: flutter
    flutter-target: lib/main.dart
